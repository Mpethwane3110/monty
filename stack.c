#include "monty.h"
/**
 * f_stack - fun that prints the top
 * @head: top stack
 * @counter: line count
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
